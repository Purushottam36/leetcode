class Solution {
    public int theMaximumAchievableX(int num, int t) {
        // No loops or recursion are needed. 
        // Direct arithmetic calculation runs instantly.
        return num + 2 * t;
    }
}